import { Text } from "react-native"
import MaskedView from 

interfaceimport { Children } from "react"
 props {
    Children: string;
}
export default function GradientText() {
    return (
        <MaskedView maskElement={<Text></Text>}></MaskedView>
            )
}